\section{Introduction}
\sectionmark{Introduction}
\label{sec:introtask} 



As neuroscience seeks to characterize the principles governing brain function, task-based functional magnetic resonance imaging (fMRI) has emerged as an important tool for investigating the relationship between cognitive processes and neural activity. Carefully controlled experimental paradigms enable the identification of functionally specialized brain regions in relatively short scanning sessions, based on the hypothesis that the blood-oxygen-level-dependent (BOLD) response in task-relevant regions will be stronger and temporally more structured than in task-irrelevant regions \citep{poldrackRegionInterestAnalysis2007, saxeDivideConquerDefense2006, gloverDeconvolutionImpulseResponse1999}.

This underlying assumption facilitates the delineation of functionally distinct cortical regions. Although the spatial location of these regions can vary substantially across individuals, their localization is generally stable within a given individual \citep{duboisBuildingScienceIndividual2016, fernandezIntrasubjectReproducibilityPresurgical2003, yoo2005long, cohenStabilityRepeatabilityExpression1999, grattonFunctionalBrainNetworks2018a}. One straightforward strategy to enhance the reliability and specificity of functional activation patterns is to increase the number of task runs, thereby improving sensitivity \citep{zandbeltSubjectVariationBOLDfMRI2008}.

Face perception is a canonical example, for which a specialized region in the human ventral temporal cortex, the fusiform face area (FFA), shows markedly increased responses to faces relative to other object categories \citep{kanwisherFusiformFaceArea1997}. Although the precise anatomical locus of the FFA within the fusiform gyrus is subject dependent, its functional selectivity for faces is highly robust across individuals \citep{bermanEvaluatingFunctionalLocalizers2010}. 

Specifically, \citet{stiglianiTemporalProcessingCapacity2015} developed a block-design visual localizer paradigm to characterize the selectivity of cortical responses to distinct stimulus categories (e.g., faces, bodies, places, objects, and orthographic characters). In this paradigm, the fusiform face area (FFA) can be localized by contrasting the response to the face stimuli versus the rest of the conditions. In the present work, we implemented an adapted language-focused version of this paradigm that additionally incorporates word stimuli, thereby enabling the assessment of both perceptual and lexical sensitivity, alongside the original face and body conditions \citep{lerma-usabiagaConvergingEvidenceFunctional2018,OHBM2026Tiger}.

Given the increasing demand for high–spatial resolution acquisitions to detect task-sensitive regions, thermal noise reduction has become essential for improving signal quality in low signal-to-noise ratio (SNR) regimes. In single-echo acquisitions, NORDIC denoising has been shown to substantially increase task-evoked response estimates within regions of interest for both visual and auditory paradigms \citep{vizioli2021lowering, dowdle2023evaluating, faesEvaluatingEffectDenoising2024}. \citet{dowdle2023evaluating} demonstrated that, for a visual task, NORDIC increased task-related sensitivity and specificity in terms of larger statistical significance and number of responsive voxels, as well as improved goodness-of-fit between time series predicted by a general linear model (given a design matrix) and the observed fMRI data.

As shown in previous chapters, multi-echo acquisition enables to enhance fMRI sensitivity and therefore represents a promising approach for improving BOLD sensitivity at higher spatial resolution \citep{kundu2012differentiating}. However, as high-spatial-resolution imaging becomes increasingly common, an optimal framework for exploiting the combined advantages of multi-echo acquisitions and thermal noise suppression has yet to be fully articulated for task-specific studies. 

This chapter investigates the most effective thermal denoising strategy for multi-echo fMRI in the context of task-based research, comparing the approaches discussed in the previous chapters, i.e., non-denoised “vanilla” data, MPPCA, NORDIC, HYDRA (i.e. spatially-concatenated NORDIC) and tensor MPPCA (TMPPCA). We assess the impact of these denoising methods on signal stability by quantifying improvements in time-series prediction based solely on design information \citep{princeImprovingAccuracySingletrial2022, dowdle2023evaluating}. Subsequently, we evaluate their influence on functional sensitivity by examining our ability to detect the fusiform face area (FFA) \citep{bermanEvaluatingFunctionalLocalizers2010} and by characterizing the magnitude of the task-related response, as estimated from model fits across runs during the response modeling stage.

\section{Methods}
\sectionmark{MethodsT}

\subsection{Data acquisition and preprocessing}
\subsectionmark{Image Acquisition Parameters}
\label{ImageParametersT}

Task-related fMRI runs were acquired on a 3T MAGNETOM PrismaFit scanner (Siemens Healthineers) equipped with a 64-channel head coil at the Basque Center on Cognition, Brain, and Language. The principal aim of this dataset is to examine the effect of thermal denoising in a functional localizer task in dataset collected at 1.5 mm voxel resolution, i.e. a regime in which thermal noise is the predominant determinant of the signal-to-noise ratio (SNR).

\textbf{ High resolution (1.5 mm) dataset.} A healthy male participant was scanned with a multi-echo fMRI sequence during four sessions comprising ten functional localizer runs (5 min per run) at 1.5 mm isotropic resolution (TR = 2 s, TEs = 16/46.92/77.84 ms, SMS = 4, partial Fourier (PF) = 6/8, GRAPPA = 2). Single-band reference (SBRef) images were acquired for each echo time (TE). For all runs, both magnitude and phase images were reconstructed. In addition, the last three noise-only volumes of each run were retained, as these data are required for the NORDIC thermal noise reduction procedure. Finally, T1-weighted MP2RAGE and T2-weighted turbo spin-echo images were acquired at 1 mm isotropic resolution.

\textbf{Experimental paradigm.} MRI data were acquired using a block-design localizer paradigm \citep{lerma-usabiagaConvergingEvidenceFunctional2018, OHBM2026Tiger}, modeled after the protocol described in \citet{stiglianiTemporalProcessingCapacity2015}. Each run comprised 51 blocks of 6 s duration, presented in randomized order (42 active blocks and 9 baseline blocks). Individual stimuli were presented for 400 ms with a 100 ms inter-stimulus interval.  The stimulus set encompassed five domains (e.g., faces, bodies, LEX, PER, words). Participants performed a one-back repetition detection task to monitor and quantify attentional engagement via response accuracy and were not considered for attention unless accuracy was equal or above $70\%$. This attentional criterion was held high to maximize the signal coming from the design, and resulted in a total of 22 analyzable runs.

\begin{figure}
    \centering
    \includegraphics[width=8cm]{images/task.png}
    \caption[Functional-localizer stimuli domain]{Functional-localizer stimuli domain. For each experimental run, 48 randomized active block encompassed one of these stimuli: \textit{words} (high \& low frequency), \textit{PER} (scrambled words \& checker boards), \textit{LEX} (consonants strings \& false fonts), \textit{faces} (adults \& children faces), \textit{bodies} (limbs \& bodies with obscured heads). Image reproduced from \citet{OHBM2026Tiger}}
    \label{fig:placeholder}
\end{figure}

\textbf{Thermal denoising.} Multi-echo fMRI data collected in each was independently denoised using different algorithms prior to minimal preprocessing: undenoised (vanilla), MP-PCA, NORDIC, NORDIC applied to spatially concatenated echoes (HYDRA), and tensor MP-PCA (TMPPCA).

\textbf{Multi-echo fMRI minimal preprocessing.} Multi-echo (ME) fMRI data were minimally preprocessed using AFNI \citep{cox1996afni}. The preprocessing pipeline included discarding the initial 10 volumes to ensure steady-state magnetization, realigning the first echo to the single-band reference (SBRef) image, and applying the resulting transformation parameters to all remaining echoes. For the high-resolution dataset, all runs were additionally aligned to the SBRef of the first run using a local Pearson correlation cost function (\texttt{align\_epi\_anat.py}, \texttt{3dAllineate}). A $T_2^*$-weighted optimal linear combination of the echoes was subsequently computed using TEDANA (multi-echo optimal combination, ME-OC) \citep{posse1999enhancement,dupre2021te}.  Finally, ME-OC voxel's timeseries were standarized to signal percentage change (i.e. substracting and dividing by the voxel's mean value), thereby ensuring comparability across runs.  
 
\textbf{Tissue-based region of interests (ROI).} Anatomical images were segmented using FreeSurfer (version 7.4.0) with the \texttt{recon-all} pipeline \citep{fischl2004automatically}. The resulting segmentations were subsequently transformed into AFNI volumetric space using the \texttt{@SUMA\_Make\_Spec\_FS} utility. A custom ROI mask was then generated with AFNI’s \texttt{3dcalc}, incorporating the bilateral cortical gray-matter ribbons as well as subcortical structures. From the SUMA output datasets, a skull-stripped anatomical volume was rigid-body aligned to the single-band reference (sbref) image of the first echo. The alignment was estimated using local Pearson correlation as the cost function. The resulting transformation matrix was then applied to the ROI mask to register it from anatomical space into functional space. 

\subsection{Multi-echo fMRI data analysis}

To assess the impact of thermal denoising on task‑based fMRI, we performed two complementary analyses. First, we quantified sensitivity to task‑evoked responses using a localizer contrast (faces vs. all other conditions). Second, we evaluated how accurately task‑related time series can be predicted from the design matrix alone, reflecting the stability and interpretability of parameter estimates. In the GLM‑prediction analysis, motion parameters were deliberately excluded as nuisance regressors—a choice that lowers goodness‑of‑fit but enables us to isolate prediction accuracy based solely on the experimental design. This analysis was extended to the whole gray matter, thereby building upon prior NORDIC work that focused exclusively on visual areas \citep{dowdle2023evaluating,vizioli2021lowering}. Both analyses are essential for determining whether denoising enhances the detectability of neural signals without introducing bias.

\begin{itemize}

\item {\textbf{Event-based general linear model (GLM):} For each denoising strategy, a general linear model (GLM), as implemented in \texttt{Nilearn 0.13.1}, and \texttt{Scikit-learn 1.7.2} with \texttt{Python 3.11}, was fitted to the timeseries of gray matter voxels.  An event-related design was specified in which hypothesized event time courses for all conditions were convolved with the canonical hemodynamic response function (HRF) provided by \texttt{Nilearn} (Glover HRF model from \citet{gloverDeconvolutionImpulseResponse1999}). Event onsets and durations were defined according to the run-specific experimental timings. In addition, up to $4^{\textrm{th}}$ order Legendre polynomials were included as nuisance regressors in a subject-specific, run-specific design matrix to perform a first-level GLM. Motion parameters were deliberately excluded as regressors to avoid capturing variance that cannot be predicted and thus cannot contribute to cross-run generalization.}

\item{\textbf{Task-localizer contrast sensitivity.} For this analysis, we focused on the contrast between the faces versus the rest of the conditions (i.e. Faces vs. rest of conditions) to identify face-selective regions, such as the fusiform face area \citep{bermanEvaluatingFunctionalLocalizers2010}, while systematically varying the number of runs included in the GLM estimation (e.g., 1, 2, 3, …). This analysis quantifies how sensitivity metrics change across denoising strategies as a function of the amount of data used for model fitting. Under the assumption that only signal components of no interest are removed, we would expect an increase in $t$-values and its spread after denoising. In addition, incorporating a greater number of runs into the GLM fit should, in principle, further increase $t$-values.}

\item {\textbf{Task-related time series prediction (GLM-prediction):} A cross-validation permutation procedure was employed to evaluate the accuracy of task-specific parameter estimates following GLM fitting for each denoising strategy
%.  See Fig.~\ref{fig:GLMreliability} for an illustration of the full procedure 
\citep{princeImprovingAccuracySingletrial2022,dowdle2023evaluating}. This analysis was designed to assess how thermal denoising strategies influence the capacity of task-specific parameters to generalize to activation patterns in independent runs, under the assumption that increased time-series sensitivity via noise reduction should improve cross-run validation. The random seed was held constant across strategies and permutations to ensure comparable train–test splits.

For each permutation, $70\%$ of the runs were randomly selected for GLM fitting (training) and the remaining $30\%$ were reserved for testing. For the test datasets, up to $4^{\textrm{th}}$-order Legendre polynomials, denoted as matrix $\mathbf{Z}$ 
%in Fig.~\ref{fig:GLMreliability}
, were regressed out prior to GLM analyses so that the residual variance could be attributed to, and thus predicted from, the experimental design alone. After GLM fitting, event-specific beta estimates were multiplied by the design matrix of the test runs to generate predicted time series for each voxel. Prediction accuracy was quantified by computing the coefficient of determination, $r^{2}$, between the predicted and observed time series for each voxel. Finally, we reported the mean per-voxel $r^{2}$ across permutations to enable comparison between denoising strategies.}
\end{itemize}

% GLM predict
%\begin{figure}[htb!]
%    \centering
%    \includegraphics[width=11cm]{images/glmpredict.pdf}
%    \caption[Task-related Time-series Prediction after linear model fitting (GLM predict)]{Flowchart of GLM-prediction analysis. Runs are split into training (70\%) and test (30\%) sets. A GLM fitted to the training data predicts the test time series. Prediction accuracy is the Pearson correlation between predicted and drift-cleaned observed signals, averaged over $i$ iterations.}
%    \label{fig:GLMreliability}
%\end{figure}

\section{Results}

\subsection{Task-localizer contrast sensitivity}

As illustrated in Fig.~\ref{fig:localizer}, the functional localizer $t$-statistic maps for faces vs. rest of conditions, and their modulation as a function of the number of runs used for GLM analyses (one, two, or three runs) exhibited increased saturation with increasing numbers of runs, as well as an interaction with the denoising method, in regions of the fusiform gyrus corresponding to the Fusiform Face Area (FFA). Among the evaluated methods, TMPPCA yields the strongest effect on $t$-values, indicating a substantial gain in sensitivity attributable to the removal of signal components unrelated to the task of interest. Consistent with the resting-state analysis, HYDRA underperforms relative to MPPCA, NORDIC and TMPPCA, further supporting the notion that the spatial redundancy introduced by spatial echo concatenation adversely affects task-related sensitivity in high-resolution acquisitions compared with these methods. Notably, all denoising methods achieve better performance with a single run than the vanilla condition does even when three runs are used.

Notably, the $t$-maps for the VANILLA condition exhibit a higher proportion of values close to zero both within and outside the FFA. In contrast, for the denoised strategies, the increase in $t$-values predominantly occurs in regions expected to be task-sensitive. This pattern indicates that variance attributable to non–task-related effects is reduced, which in turn yields higher $t$-statistics for task-relevant effects.

\begin{figure}[htbp]
\raggedleft\colorbox{black}{
    \raggedleft
    \begin{tabular}{>{\centering\arraybackslash}m{.7cm}>{\centering\arraybackslash}m{3cm}>{\centering\arraybackslash}m{3cm}>{\centering\arraybackslash}m{3cm}>{\centering\arraybackslash}m{.7cm}}
    
       & \textcolor{white}{\textbf{1 run}} & \textcolor{white}{\textbf{2 runs}} &\textcolor{white}{\textbf{3 runs}} \\
   \rotatebox[origin=l]{90}{\textcolor{white}{VANILLA}}
  &    \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_vanilla_localizer1_all} &     \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_vanilla_localizer2_all} &
      \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_vanilla_localizer3_all}& \\
   \rotatebox[origin=l]{90}{\textcolor{white}{MPPCA}}
  &    \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_mppca_localizer1_all} &     \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_mppca_localizer2_all} &
      \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_mppca_localizer3_all}&\\
   \rotatebox[origin=l]{90}{\textcolor{white}{NORDIC}}
  &    \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_nordic_localizer1_all} &     \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_nordic_localizer2_all} &
      \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_nordic_localizer3_all}&\textcolor{white}{\textbf{$t$}} \includegraphics[width=.5cm]{images/yellow2cyan_t.png}\\
   \rotatebox[origin=l]{90}{\textcolor{white}{HYDRA}}
  &    \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_hydra_localizer1_all} &     \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_hydra_localizer2_all} &
      \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_hydra_localizer3_all}&\\
   \rotatebox[origin=l]{90}{\textcolor{white}{TMPPCA}}
  &    \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_tmppca_localizer1_all} &     \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_tmppca_localizer2_all} &
      \includegraphics[width=3cm]{images/sub-001_ses-1_task-fLoc_run-1_OC_part-mag_bold_tmppca_localizer3_all}&\\
    \end{tabular}
    }
    \caption[Functional localizer t-statistic maps (faces vs. all other conditions) and their modulation as a function of the number of runs]{Functional localizer t-maps (faces $>$ others) and their modulation as a function of the number of runs for all the denoising conditions. Maps are thresholded at $t = 1.8$, $p = 0.05$.}
    \label{fig:localizer}
\end{figure} 


\subsection{GLM prediction}
\begin{figure}[htbp]
\centering \colorbox{black}{
    \centering
    \begin{tabular}{>{\centering\arraybackslash}m{1cm}>{\centering\arraybackslash}m{5cm}>{\centering\arraybackslash}m{5cm}}
       & \textcolor{white}{\textbf{VANILLA}} & \textcolor{white}{\textbf{MPPCA}} \\
    &    \includegraphics[width=5cm]{images/vanilla_task_real_mean_r2_all.jpg} &
        \includegraphics[width=5cm]{images/mppca_task_real_mean_r2_all.jpg}\\
      &   \textcolor{white}{\textbf{NORDIC}}& \textcolor{white}{\textbf{HYDRA}}\\
        \includegraphics[width=.5cm]{images/Plasmaglm.png}&\includegraphics[width=5cm]{images/nordic_task_real_mean_r2_all.jpg} &
        \includegraphics[width=5cm]{images/hydra_task_real_mean_r2_all.jpg}\\
          &       \multicolumn{2}{c}{\textcolor{white}{\textbf{TMPPCA}}}\\
      &  \multicolumn{2}{c}{\includegraphics[width=5cm]{images/tmppca_task_real_mean_r2_all.jpg}} \\
    \end{tabular}
    }
    \caption[Average goodness-of-fit ($r^2$) between minimally preprocessed time series and voxel-wise predictions]{Average goodness-of-fit ($r^2$) between minimally preprocessed time series and GLM predictions (100 iterations). Thermal denoising increases $r^2$; MPPCA, NORDIC, and TMPPCA all yield higher saturation, with TMPPCA highest overall.} 
    \label{fig:glmPredictr2}  
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=12cm]{images/halfviolin_glm_across_subjects_masked.png}
    \caption[Distribution of goodnes-of-fit score, $r^2$ after time-series prediction across 100 iterations per method]{Distribution of $r^2$ (100 iterations). Denoising increases $r^2$, shifting from unimodal to bimodal. TMPPCA $>$ NORDIC $> $MPPCA; HYDRA below vanilla, consistent with 1.5 mm findings.}
    \label{fig:glmdist}
\end{figure}


    
We evaluated the efficacy of thermal denoising in preserving task‑related signal by quantifying the discrepancy between design‑matrix‑predicted and empirically observed time series. The analysis comprised 100 iterations and was restricted to the 22 runs with the highest behavioral accuracy.

Mean $r^2$ maps for each denoising strategy demonstrate clear differences in model goodness-of-fit (Fig.~\ref{fig:glmPredictr2}). Using VANILLA as the reference condition, a substantial improvement in $r^2$ is observed following thermal denoising, with overall performance following the pattern HYDRA $<$ VANILLA $<$ MPPCA $<$ NORDIC $<$ TMPPCA. In agreement with the resting-state analysis at 1.5 mm isotropic resolution, NORDIC outperforms HYDRA. 
%These results provide strong evidence that increasing spatial redundancy in NORDIC denoising does not improve, and may in fact attenuate, the task-relevant signal \citep{fernandesMPPCADenoisingFMRI2023a, manzanopatronDenoisingDiffusionMRI2024}.

For the VANILLA dataset, the highest $r^2$ scores were localized to task-relevant cortical regions (e.g., visual cortex), as expected given that these areas most strongly reflect design-induced signal changes \citep{lerma-usabiagaConvergingEvidenceFunctional2018,stiglianiTemporalProcessingCapacity2015}. For denoising strategies that yielded increased $r^2$ values, the improvement in goodness-of-fit was not restricted to regions with high task-evoked responses but extended across the entire gray matter. This pattern indicates enhanced robustness of the underlying signal, leading to more reliable beta estimates and reduced random signal fluctuations.

As shown in Fig.~\ref{fig:glmdist}, the distribution of goodness-of-fit scores ($r^2$) from time-series prediction further supports the benefit of thermal denoising, as evidenced by heavier distribution tails skewed towards higher values. Thermal denoising consistently increases $r^2$, as reflected by heavier tails skewed toward higher values. By reducing variance \citep{kay2022riskL, comby2023denoising}, denoising shifts the distribution away from unimodality toward bimodality, presumably with gains predominantly affecting voxels likely reflecting a clearer separation between task-sensitive and non–task-sensitive voxels. TMPPCA yields the largest improvement, followed by NORDIC and MPPCA. Notably, HYDRA underperforms relative to the vanilla condition, consistent with the observations in resting-state data at this voxel resolution (1.5 mm isotropic).

Therefore, this analysis highlights that multi-echo thermal denoising with MPPCA, NORDIC and, above all, TMPPCA  enhances the proportion of variance in the BOLD time series that can be attributed to the experimental design alone. 


\section{Discussion}

In the previous chapter, we demonstrated that TMPPCA outperforms NORDIC, HYDRA, and MPPCA on signal quality metrics in resting‑state acquisitions (see Chap. \ref{chap:denoising}). Here, we extend this evaluation to task‑based data, assessing the extent to which multi-echo thermal denoising enhances sensitivity to task‑evoked brain activity, thereby improving the utility of functional imaging for neuroscience \citep{poldrackRegionInterestAnalysis2007}. Consistent with prior findings, multi-echo thermal denoising is expected to remove variance unrelated to the experimental manipulation, it should yield data that are more amenable to linear modeling for effect detection \citep{zhu2022denoise,comby2023denoising}.

As anticipated, multi-echo thermal denoising increased sensitivity to task‑specific activations by enhancing both the magnitude and extent of responsive voxels of the fusiform face area (FFA) at the single-subject level \citep{kanwisherFusiformFaceArea1997}. Across all evaluations, TMPPCA, followed by NORDIC, provided the most favorable results, producing higher and more spatially localized $t$‑statistics relative to undenoised data. However, this increased sensitivity also raises the possibility that some activations may reflect statistical artifacts introduced by denoising procedures \citep{kay2022riskL,fernandesMPPCADenoisingFMRI2023a}. 

To overcome this limitation, the GLM prediction analysis quantifies the amount of signal that can be predicted from the design matrix alone, as well as the accuracy with which test time series can be predicted from their corresponding design \citep{princeImprovingAccuracySingletrial2022}. In agreement with reports from single‑echo acquisitions, NORDIC improves the goodness of fit between predicted and observed time series, outperforming MPPCA and HYDRA \citep{dowdle2023evaluating}. However, we found that TMPPCA, applied in the tensorial domain, yields the best overall performance \citep{olesenTensorDenoisingMultidimensional2023}. Multi-echo hermal denoising induced a transformation in the distribution of voxelwise $r^2$ values from unimodal to bimodal. Given that not all gray‑matter voxels are expected to be task sensitive, reliability scores should preferentially increase in task‑selective regions. This selective enhancement is consistent with the emergence of a bimodal distribution, and this shift was most pronounced for TMPPCA.

Nevertheless, the average goodness-of-fit ($r^2$) was very low for all conditions (see the range in Fig.~\ref{fig:glmdist}). We conjecture that, as this evaluation is based on prediction of the entire time series rather than on task-specific epochs alone, residual confounds such as motion-related artifacts may still adversely affect signal predictability.

\section{Conclusions}
 
This chapter investigated the impact of thermal denoising on multi‑echo sensitivity to BOLD‑induced task-related effects. Our analyses showed that multi-echo thermal denoising increases the proportion of variance explained by the task design alone. Specifically, TMPPCA resulted in the largest improvement, followed by NORDIC and MPPCA, in task-related activation mapping and signal predictability.

 

